---
phase: 03-daily-checklist-app
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - /Users/josi/rez-checklist/src/app/page.tsx
  - /Users/josi/rez-checklist/src/app/checklist/page.tsx
  - /Users/josi/rez-checklist/src/components/LocationShiftSelector.tsx
  - /Users/josi/rez-checklist/src/components/NameEntry.tsx
  - /Users/josi/rez-checklist/src/components/ChecklistCategory.tsx
  - /Users/josi/rez-checklist/src/components/ChecklistItem.tsx
autonomous: true
requirements: [CHK-01, CHK-02, CHK-04, CHK-05, CHK-09]

must_haves:
  truths:
    - "Staff can tap one of 5 locations, then tap Opening or Closing, in under 5 seconds"
    - "Staff can type their name and proceed to the checklist"
    - "Checklist shows items grouped by category, filtered to the selected location and shift"
    - "Tapping an item marks it complete immediately (optimistic) with automatic timestamp"
    - "Staff can expand any item to add an optional note before or after completing it"
    - "All tap targets are at least 56px tall for tablet use"
    - "Session info (location, shift, name) visible in header on checklist page"
    - "Page refresh returns to session-start screen (no ghost session)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Session-start screen with location selector, shift selector, name entry"
      min_lines: 40
    - path: "src/app/checklist/page.tsx"
      provides: "Checklist page rendering categorized items with session header"
      min_lines: 30
    - path: "src/components/LocationShiftSelector.tsx"
      provides: "Large-button grid for 5 locations + 2 shift types"
    - path: "src/components/NameEntry.tsx"
      provides: "Name text input with proceed button"
    - path: "src/components/ChecklistItem.tsx"
      provides: "Tappable item with optimistic complete, expandable notes"
    - path: "src/components/ChecklistCategory.tsx"
      provides: "Category group rendering filtered items"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/context/SessionContext.tsx"
      via: "useSession() to set location, shift, name"
      pattern: "useSession"
    - from: "src/app/checklist/page.tsx"
      to: "src/lib/checklist-config.ts"
      via: "getItemsForSession(location, shift)"
      pattern: "getItemsForSession"
    - from: "src/components/ChecklistItem.tsx"
      to: "src/actions/completions.ts"
      via: "appendCompletion() on tap"
      pattern: "appendCompletion"
    - from: "src/app/page.tsx"
      to: "/checklist"
      via: "router.push after session ready"
      pattern: "router\\.push.*checklist"
---

<objective>
Build the complete staff-facing checklist flow: session-start screen (location + shift + name) and checklist page (categorized items with tap-to-complete, optimistic UI, and optional notes).

Purpose: This is the core user flow -- what restaurant staff interact with every shift. Must be fast, obvious, impossible to misuse on a shared tablet.

Output: Working staff flow from session start to checklist completion, writing to Google Sheets on every item tap.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-daily-checklist-app/03-CONTEXT.md
@.planning/phases/03-daily-checklist-app/03-RESEARCH.md
@.planning/phases/03-daily-checklist-app/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/types.ts:
```typescript
export type ShiftType = 'Opening' | 'Closing';
export type Category = 'Opening Procedures' | 'Closing Procedures' | 'Cleaning & Sanitation' | 'Equipment Checks';
export const LOCATIONS = ['Moto Medi Lubbock 1', 'Moto Medi Lubbock 2', 'Moto Medi Amarillo', 'Tikka Shack 1', 'Tikka Shack 2'] as const;
export type Location = typeof LOCATIONS[number];
export interface CompletionEvent { location: string; shift: ShiftType; staffName: string; itemId: string; itemText: string; category: string; notes: string; }
```

From src/lib/checklist-config.ts:
```typescript
export interface ChecklistItem { id: string; category: Category; shift: ShiftType | 'Both'; text: string; locationOverride?: string[]; }
export function getItemsForSession(location: string, shift: ShiftType): ChecklistItem[];
```

From src/actions/completions.ts:
```typescript
export async function appendCompletion(params: CompletionEvent): Promise<{ success: true; timestamp: string }>;
```

From src/context/SessionContext.tsx:
```typescript
export function useSession(): {
  location: Location | null; shift: ShiftType | null; staffName: string | null;
  setLocation: (loc: Location) => void; setShift: (shift: ShiftType) => void;
  setStaffName: (name: string) => void; reset: () => void; isReady: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build session-start screen (location + shift + name entry)</name>
  <files>
    /Users/josi/rez-checklist/src/app/page.tsx
    /Users/josi/rez-checklist/src/components/LocationShiftSelector.tsx
    /Users/josi/rez-checklist/src/components/NameEntry.tsx
  </files>
  <action>
    The session-start screen is the #1 UX priority. Ghost sessions are the biggest risk -- this screen must be unmistakably clear and always the first thing staff see.

    1. Create `src/components/LocationShiftSelector.tsx` ('use client'):
       - Import LOCATIONS and ShiftType from types.ts, useSession from context
       - Three-step flow rendered as a single page with visual progression:
         - Step 1: Location grid -- 5 large buttons (one per location), minimum 72px tall, full-width on tablet. Moto Medi locations grouped visually (e.g., with a subtle brand-color header), Tikka Shack locations grouped separately.
         - Step 2: Shift selector -- 2 large buttons (Opening / Closing), only visible after location selected. Use distinct icons or colors for each (e.g., sunrise orange for Opening, moon/navy for Closing).
         - Step 3: Calls onComplete callback when both selected
       - Selected state: bold visual feedback (filled background, checkmark or border change). Not subtle -- make it obvious.
       - Allow changing selection by tapping a different button (deselect previous).
       - Use CSS transitions for step reveals (staggered animation-delay for button entrance).

    2. Create `src/components/NameEntry.tsx` ('use client'):
       - Large text input (min height 56px, large font) with placeholder "Enter your name"
       - "Start Checklist" button below (large, prominent, accent color)
       - Button disabled until name is at least 2 characters
       - On submit: calls useSession().setStaffName(name)
       - Auto-focus the input on mount (tablet keyboard should appear)

    3. Update `src/app/page.tsx` ('use client'):
       - Import useSession, useRouter
       - If session.isReady, redirect to /checklist via router.push
       - Otherwise render the session-start flow:
         - If no location/shift selected: show LocationShiftSelector
         - If location+shift selected but no name: show NameEntry with a header showing selected location + shift (so staff can verify before entering name)
       - Include a prominent title/header: "Shift Checklist" or similar
       - Show the current date prominently
       - Full-screen layout, centered content, atmospheric background (use the CSS variables from globals.css)
       - Include a "Start Fresh" or reset link in case of ghost session detection

    AESTHETICS (per CLAUDE.md -- no AI slop):
    - Use the distinctive font from layout.tsx
    - Large, bold typography for the title and button labels
    - Location buttons should feel tactile -- shadows, hover states, active press states
    - Consider a warm industrial palette: deep charcoal backgrounds, copper/amber accents, cream text
    - Staggered entrance animation for the location buttons (CSS animation-delay)
    - The overall feel: like a well-designed kitchen display system, not a generic form

    After creating the production code, fill in the Wave 0 test stubs:
    - `src/__tests__/LocationShiftSelector.test.tsx`: Implement all test.todo items — render component, verify 5 location buttons present, verify shift buttons appear after location tap, verify onComplete callback.
    - `src/__tests__/NameEntry.test.tsx`: Implement all test.todo items — render component, verify disabled state with short name, verify setStaffName called on submit.
  </action>
  <verify>
    <automated>cd /Users/josi/rez-checklist && npm run build && npx jest src/__tests__/LocationShiftSelector.test.tsx src/__tests__/NameEntry.test.tsx --passWithNoTests</automated>
    Build exits 0. No type errors. Tests pass. `grep -r "useSession" src/app/page.tsx src/components/` confirms session context usage.
  </verify>
  <done>
    Session-start screen renders with 5 location buttons and 2 shift buttons. Name entry appears after location+shift selected. Selecting all three sets session and redirects to /checklist. Page refresh returns to this screen (React state only). All tap targets >= 56px. Distinctive, non-generic styling applied. LocationShiftSelector and NameEntry tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build checklist page with categorized items, optimistic tap-to-complete, and notes</name>
  <files>
    /Users/josi/rez-checklist/src/app/checklist/page.tsx
    /Users/josi/rez-checklist/src/components/ChecklistItem.tsx
    /Users/josi/rez-checklist/src/components/ChecklistCategory.tsx
  </files>
  <action>
    1. Create `src/components/ChecklistItem.tsx` ('use client'):
       - Props: ChecklistItem data + session info (location, shift, staffName)
       - State: completed (boolean), pending (boolean), notes (string), showNotes (boolean)
       - Tap handler (optimistic UI pattern from RESEARCH.md):
         - If already completed or pending, ignore tap
         - Set completed=true immediately (optimistic)
         - Set pending=true
         - Call appendCompletion({ location, shift, staffName, itemId, itemText, category, notes })
         - On success: pending=false (stay completed)
         - On failure: revert completed=false, show brief error indicator (red flash or text)
         - Finally: pending=false
       - Notes toggle: small icon/button to expand notes textarea below the item
         - Notes textarea: min 56px height, placeholder "Add a note..."
         - Notes are included in the appendCompletion call (empty string if none)
         - Notes can be entered before or after completing the item
       - Visual states:
         - Uncompleted: card with item text, muted background
         - Completed: green/success background, checkmark icon, timestamp shown, slightly reduced opacity or strikethrough
         - Pending: subtle pulse animation or spinner icon
         - Failed: red border flash
       - Min height 64px per item for easy tablet tapping

    2. Create `src/components/ChecklistCategory.tsx` ('use client'):
       - Props: category name (string), items (ChecklistItem[]), session info
       - Renders category header (bold, slightly larger text) with item count and completion count
       - Maps items to ChecklistItem components
       - Shows progress indicator: "3/5 complete" or a small progress bar
       - Category header has distinct styling per category (subtle color accent or icon)

    3. Create `src/app/checklist/page.tsx` ('use client'):
       - Import useSession, useRouter, getItemsForSession
       - Guard: if !session.isReady, redirect to / (prevents direct URL access without session)
       - Sticky header showing: location name, shift type, staff name, current date
         - Header includes a "New Session" button that calls session.reset() and navigates to /
         - The header makes ghost sessions immediately detectable -- staff see whose session is active
       - Body: group items by category using getItemsForSession(location, shift)
         - Render each category with ChecklistCategory component
         - Categories in order: Opening Procedures (if Opening shift), Closing Procedures (if Closing), Cleaning & Sanitation, Equipment Checks
       - Bottom of page: completion summary ("12/15 items complete")
       - When all items complete, show a congratulatory state (e.g., "All done! Great shift." with a checkmark)
       - Scroll behavior: smooth, natural. Categories should be visually separated.

    AESTHETICS:
    - Checklist items should feel like physical cards -- subtle shadow, rounded corners, satisfying tap feedback
    - Completed items: smooth transition to success state (CSS transition on background-color, ~200ms)
    - Category headers: bold but not overwhelming, clear visual separation
    - The overall feel: clean, efficient, purposeful -- a tool you want to use, not a chore
    - Progress indicators use accent color from CSS variables
    - Consider a subtle celebration animation when all items complete (CSS keyframes, nothing heavy)

    After creating the production code, fill in the Wave 0 test stubs:
    - `src/__tests__/ChecklistItem.test.tsx`: Implement all test.todo items — render component, verify item text, mock appendCompletion, test optimistic state on tap, test revert on failure, test tap ignored when completed.
    - `src/__tests__/ChecklistItem.notes.test.tsx`: Implement all test.todo items — test expand button toggles textarea, test notes value included in appendCompletion call, test empty string when no note.
  </action>
  <verify>
    <automated>cd /Users/josi/rez-checklist && npm run build && npx jest src/__tests__/ChecklistItem.test.tsx src/__tests__/ChecklistItem.notes.test.tsx --passWithNoTests</automated>
    Build exits 0. Tests pass. `grep -r "appendCompletion" src/components/ChecklistItem.tsx` confirms Server Action usage. `grep -r "getItemsForSession" src/app/checklist/` confirms config usage. `grep -r "session.isReady\|isReady" src/app/checklist/` confirms session guard.
  </verify>
  <done>
    Checklist page renders categorized items filtered by location+shift. Tapping an item marks it complete optimistically and fires appendCompletion Server Action. Notes expandable on any item. Session header shows location/shift/name persistently. "New Session" button resets and returns to start. Direct URL access without session redirects to /. All items >= 56px tap targets. Build passes clean. ChecklistItem and ChecklistItem.notes tests pass.
  </done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 in /Users/josi/rez-checklist/
- `npx jest src/__tests__/LocationShiftSelector.test.tsx src/__tests__/NameEntry.test.tsx src/__tests__/ChecklistItem.test.tsx src/__tests__/ChecklistItem.notes.test.tsx` all pass
- Session-start screen shows 5 locations, 2 shifts, name entry (3-step flow)
- Selecting location+shift+name redirects to /checklist
- Checklist page guards against missing session (redirects to /)
- ChecklistItem calls appendCompletion with correct params including notes
- Optimistic UI pattern: completed state set before Server Action resolves
- Session header on checklist page shows location, shift, staff name
- "New Session" button resets context and navigates to /
- All interactive elements >= 56px tap target height
- No localStorage or sessionStorage usage
</verification>

<success_criteria>
Complete staff-facing flow works end-to-end: start session -> see checklist -> tap items to complete -> items write to Google Sheets. The UI is tablet-optimized, distinctively styled, and ghost-session-resistant. All component tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-checklist-app/03-02-SUMMARY.md`
</output>
