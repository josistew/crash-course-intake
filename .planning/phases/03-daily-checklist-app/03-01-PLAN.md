---
phase: 03-daily-checklist-app
plan: 01
type: execute
wave: 1
depends_on: [03-00]
files_modified:
  - /Users/josi/rez-checklist/package.json
  - /Users/josi/rez-checklist/tsconfig.json
  - /Users/josi/rez-checklist/postcss.config.mjs
  - /Users/josi/rez-checklist/src/app/layout.tsx
  - /Users/josi/rez-checklist/src/app/globals.css
  - /Users/josi/rez-checklist/src/lib/sheets.ts
  - /Users/josi/rez-checklist/src/lib/checklist-config.ts
  - /Users/josi/rez-checklist/src/lib/types.ts
  - /Users/josi/rez-checklist/src/actions/completions.ts
  - /Users/josi/rez-checklist/src/context/SessionContext.tsx
  - /Users/josi/rez-checklist/.env.local
  - /Users/josi/rez-checklist/.gitignore
autonomous: true
requirements: [CHK-03, CHK-06, CHK-09]

must_haves:
  truths:
    - "Next.js project scaffolded in /Users/josi/rez-checklist with Tailwind v4, TypeScript, App Router"
    - "Google Sheets auth works with read-write scope (not readonly like LeaseJenny)"
    - "appendCompletion Server Action builds correct row and calls sheets.spreadsheets.values.append with INSERT_ROWS"
    - "Checklist config returns correct items filtered by location and shift"
    - "Session context provides location, shift, staffName via React context (no localStorage)"
    - "Viewport meta prevents pinch-zoom on tablet"
  artifacts:
    - path: "src/lib/sheets.ts"
      provides: "Google Sheets auth with read-write scope"
      contains: "googleapis"
    - path: "src/lib/checklist-config.ts"
      provides: "Hardcoded checklist items, locations, types"
      exports: ["CHECKLIST_ITEMS", "LOCATIONS", "ChecklistItem", "ShiftType", "Category"]
    - path: "src/lib/types.ts"
      provides: "Shared TypeScript types for session, completion events"
    - path: "src/actions/completions.ts"
      provides: "Server Action for appending completion rows to Sheets"
      exports: ["appendCompletion"]
    - path: "src/context/SessionContext.tsx"
      provides: "React context for location, shift, staffName (in-memory only)"
    - path: "src/app/layout.tsx"
      provides: "Root layout with viewport meta, distinctive font, SessionProvider"
  key_links:
    - from: "src/actions/completions.ts"
      to: "src/lib/sheets.ts"
      via: "imports getAuth/getSheets"
      pattern: "sheets\\.spreadsheets\\.values\\.append"
    - from: "src/actions/completions.ts"
      to: "src/lib/checklist-config.ts"
      via: "uses types"
    - from: "src/app/layout.tsx"
      to: "src/context/SessionContext.tsx"
      via: "wraps children in SessionProvider"
---

<objective>
Scaffold the rez-checklist Next.js project with all data layer foundations: Google Sheets auth (read-write scope), checklist config, Server Action for completions, session context, and tablet-locked viewport.

Purpose: Establish the entire data and type contract so Plan 02 (staff UI) and Plan 03 (manager dashboard) can build against stable interfaces without exploring the codebase.

Output: A buildable Next.js project at /Users/josi/rez-checklist/ with all server-side logic, types, and config ready for UI consumption.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-daily-checklist-app/03-CONTEXT.md
@.planning/phases/03-daily-checklist-app/03-RESEARCH.md

Reference the LeaseJenny sheets pattern:
@/Users/josi/leasejenny-dashboard/src/lib/sheets.ts

<interfaces>
<!-- Types and contracts this plan CREATES — Plans 02 and 03 consume these directly. -->

From src/lib/types.ts (to be created):
```typescript
export type ShiftType = 'Opening' | 'Closing';
export type Category = 'Opening Procedures' | 'Closing Procedures' | 'Cleaning & Sanitation' | 'Equipment Checks';
export type Location = typeof LOCATIONS[number];

export interface SessionState {
  location: Location | null;
  shift: ShiftType | null;
  staffName: string | null;
}

export interface CompletionEvent {
  location: string;
  shift: ShiftType;
  staffName: string;
  itemId: string;
  itemText: string;
  category: string;
  notes: string;
}
```

From src/lib/checklist-config.ts (to be created):
```typescript
export interface ChecklistItem {
  id: string;
  category: Category;
  shift: ShiftType | 'Both';
  text: string;
  locationOverride?: string[];
}

export const LOCATIONS: readonly string[];
export const CHECKLIST_ITEMS: ChecklistItem[];
export function getItemsForSession(location: string, shift: ShiftType): ChecklistItem[];
```

From src/actions/completions.ts (to be created):
```typescript
export async function appendCompletion(params: CompletionEvent): Promise<{ success: true; timestamp: string }>;
```

From src/context/SessionContext.tsx (to be created):
```typescript
export function SessionProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useSession(): SessionState & {
  setLocation: (loc: Location) => void;
  setShift: (shift: ShiftType) => void;
  setStaffName: (name: string) => void;
  reset: () => void;
  isReady: boolean; // true when location + shift + staffName all set
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with Tailwind v4 and core dependencies</name>
  <files>
    /Users/josi/rez-checklist/package.json
    /Users/josi/rez-checklist/tsconfig.json
    /Users/josi/rez-checklist/postcss.config.mjs
    /Users/josi/rez-checklist/src/app/layout.tsx
    /Users/josi/rez-checklist/src/app/globals.css
    /Users/josi/rez-checklist/src/app/page.tsx
    /Users/josi/rez-checklist/.env.local
    /Users/josi/rez-checklist/.gitignore
  </files>
  <action>
    1. Run `npx create-next-app@latest rez-checklist --typescript --tailwind --app --src-dir --no-import-alias` in /Users/josi/
    2. cd into /Users/josi/rez-checklist and run `npm install googleapis`
    3. Verify Tailwind is v4 in package.json (NOT v3 -- v3 has Turbopack bug). If create-next-app installed v3, uninstall and install `tailwindcss@^4 @tailwindcss/postcss@^4`
    4. Create `.env.local` with placeholder vars:
       ```
       GOOGLE_SERVICE_ACCOUNT_KEY=your-base64-encoded-key
       CHECKLIST_SHEET_ID=your-sheet-id
       ```
    5. Update `src/app/layout.tsx`:
       - Export `viewport` object (separate from metadata) with: width device-width, initialScale 1, minimumScale 1, maximumScale 1, userScalable false
       - Import a distinctive Google Font -- choose something bold and industrial that fits a restaurant ops tool (NOT Inter, Roboto, Arial, Space Grotesk, or any generic font). Consider fonts like Outfit, Syne, DM Sans, Lexend, or Anybody. The font should be highly readable on tablet screens at various sizes.
       - Set metadata title to "Rez Checklist"
       - Wrap children in SessionProvider (from Task 2)
    6. Update `src/app/globals.css` with CSS variables for a cohesive theme. Pick a bold, restaurant-ops aesthetic -- think warm industrial (deep charcoals, warm amber/copper accents) or crisp kitchen (clean whites with bold accent). NOT purple gradients on white. Define CSS variables for --bg, --fg, --accent, --accent-hover, --card, --card-border, --success, --warning, --muted. Add layered background (subtle gradient or pattern, not flat solid).
    7. Replace the default `src/app/page.tsx` with a placeholder that says "Rez Checklist" (Plan 02 will build the real session-start screen here).
    8. Ensure .gitignore includes .env.local and node_modules.
  </action>
  <verify>
    <automated>cd /Users/josi/rez-checklist && npm run build</automated>
    Build should exit 0 with no errors. Check that Tailwind v4 is in package.json.
  </verify>
  <done>
    Next.js project builds clean at /Users/josi/rez-checklist/. Tailwind v4 installed. googleapis installed. Viewport meta prevents pinch-zoom. Distinctive font loaded. CSS variables define cohesive restaurant-ops theme. .env.local has placeholder vars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create data layer — types, checklist config, Sheets auth, Server Action, session context</name>
  <files>
    /Users/josi/rez-checklist/src/lib/types.ts
    /Users/josi/rez-checklist/src/lib/sheets.ts
    /Users/josi/rez-checklist/src/lib/checklist-config.ts
    /Users/josi/rez-checklist/src/actions/completions.ts
    /Users/josi/rez-checklist/src/context/SessionContext.tsx
  </files>
  <action>
    1. Create `src/lib/types.ts` with all shared types:
       - ShiftType = 'Opening' | 'Closing'
       - Category = 'Opening Procedures' | 'Closing Procedures' | 'Cleaning & Sanitation' | 'Equipment Checks'
       - LOCATIONS const array: 'Moto Medi Lubbock 1', 'Moto Medi Lubbock 2', 'Moto Medi Amarillo', 'Tikka Shack 1', 'Tikka Shack 2'
       - Location type derived from LOCATIONS
       - SessionState interface (location, shift, staffName -- all nullable)
       - CompletionEvent interface (location, shift, staffName, itemId, itemText, category, notes)

    2. Create `src/lib/sheets.ts` — mirror LeaseJenny pattern at /Users/josi/leasejenny-dashboard/src/lib/sheets.ts but with these differences:
       - Scope MUST be 'https://www.googleapis.com/auth/spreadsheets' (read-write, NOT readonly)
       - Use CHECKLIST_SHEET_ID env var (not GOOGLE_SHEET_ID)
       - Export getAuth() and getSheets() helpers for reuse by Server Action and API routes
       - Export a readTab(tabName: string, range: string) helper that fetches a tab range and returns string[][]

    3. Create `src/lib/checklist-config.ts`:
       - Import types from types.ts
       - Define ChecklistItem interface: { id, category, shift ('Opening'|'Closing'|'Both'), text, locationOverride? }
       - Create CHECKLIST_ITEMS array with placeholder items (3-4 per category, ~15 total). These are placeholders until Rez provides actual SOPs. Include:
         - Opening Procedures (shift: 'Opening'): unlock doors, check POS register, verify food temps, prep station setup
         - Closing Procedures (shift: 'Closing'): lock doors, close out POS, clean grease traps, take out trash
         - Cleaning & Sanitation (shift: 'Both'): sanitize prep surfaces, clean restrooms, sweep/mop floors
         - Equipment Checks (shift: 'Both'): check walk-in cooler temp, ice machine status, verify hood vent operation
       - Export getItemsForSession(location: string, shift: ShiftType): ChecklistItem[] — filters by shift match (exact or 'Both') and locationOverride

    4. Create `src/actions/completions.ts`:
       - 'use server' directive at top
       - Import getAuth and sheets from sheets.ts
       - Export appendCompletion(params: CompletionEvent): Promise<{ success: true; timestamp: string }>
       - Build row array: [timestamp, location, shift, staffName, itemId, itemText, category, notes]
       - Call spreadsheets.values.append with:
         - range: "'Completions-Log'!A:H"
         - valueInputOption: 'USER_ENTERED'
         - insertDataOption: 'INSERT_ROWS' (NOT OVERWRITE -- safer for concurrent writes)
       - Implement exponential backoff: retry up to 3 times on HTTP 429, delay = 2^attempt * 500ms
       - Return { success: true, timestamp }

    5. Create `src/context/SessionContext.tsx`:
       - 'use client' directive at top
       - React context with SessionState + setters (setLocation, setShift, setStaffName, reset)
       - Computed `isReady` boolean: true when all three fields are non-null
       - reset() clears all fields back to null (used for "new session" button)
       - NO localStorage, NO sessionStorage -- React state only (prevents ghost sessions)
       - Export SessionProvider and useSession hook

    6. Update layout.tsx to import and wrap children with SessionProvider.

    After creating the production code, fill in the Wave 0 test stubs that correspond to this plan's code:
    - `src/__tests__/checklist-config.test.ts`: Implement all test.todo items — test that CHECKLIST_ITEMS has items across all 4 categories, getItemsForSession filters correctly by shift, includes 'Both' items, respects locationOverride, LOCATIONS has 5 entries.
    - `src/__tests__/actions.completions.test.ts`: Implement all test.todo items — mock googleapis, test row array construction, INSERT_ROWS usage, backoff on 429.
    - `src/__tests__/layout.test.tsx`: Implement all test.todo items — import the viewport export from layout.tsx and assert maximumScale=1, userScalable=false.
  </action>
  <verify>
    <automated>cd /Users/josi/rez-checklist && npm run build && npx jest src/__tests__/checklist-config.test.ts src/__tests__/actions.completions.test.ts src/__tests__/layout.test.tsx --passWithNoTests</automated>
    Build should exit 0. Tests should pass. `grep -r "INSERT_ROWS" src/actions/` returns a match. `grep -r "spreadsheets.readonly" src/` returns NO matches (must be read-write scope). `grep -r "localStorage\|sessionStorage" src/` returns NO matches.
  </verify>
  <done>
    All data layer files exist and compile. sheets.ts uses read-write scope. appendCompletion uses INSERT_ROWS with exponential backoff. checklist-config.ts has ~15 placeholder items across 4 categories. SessionContext uses React state only (no persistence). Build passes clean. Tests for checklist-config, actions.completions, and layout pass.
  </done>
</task>

</tasks>

<verification>
- `cd /Users/josi/rez-checklist && npm run build` exits 0
- `npx jest src/__tests__/checklist-config.test.ts src/__tests__/actions.completions.test.ts src/__tests__/layout.test.tsx` all pass
- Tailwind v4 confirmed in package.json (not v3)
- googleapis in package.json dependencies
- No `spreadsheets.readonly` anywhere in src/ (must be read-write)
- No `localStorage` or `sessionStorage` anywhere in src/
- `INSERT_ROWS` used in completions action (not OVERWRITE)
- Viewport meta includes maximumScale=1, userScalable=false
- CSS variables defined for theme colors
- Distinctive font loaded (not Inter/Roboto/Arial/Space Grotesk)
</verification>

<success_criteria>
The rez-checklist project at /Users/josi/rez-checklist/ builds successfully with all data layer contracts in place. Plan 02 can import types, config, actions, and context without creating any new server-side code. Plan 03 can import sheets.ts readTab helper for dashboard reads. Corresponding test stubs are implemented and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-checklist-app/03-01-SUMMARY.md`
</output>
