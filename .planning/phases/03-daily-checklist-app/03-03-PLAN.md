---
phase: 03-daily-checklist-app
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - /Users/josi/rez-checklist/src/app/dashboard/page.tsx
  - /Users/josi/rez-checklist/src/app/api/dashboard/route.ts
  - /Users/josi/rez-checklist/src/components/ManagerDashboard.tsx
autonomous: false
requirements: [CHK-07, CHK-08]

must_haves:
  truths:
    - "Manager can visit /dashboard and see today's checklist status across all 5 locations"
    - "Dashboard shows each location with shift status: complete, in progress, or not started"
    - "Dashboard reads from Manager-View tab (pre-computed QUERY in Sheet), not raw Completions-Log"
    - "Dashboard handles empty state gracefully (no activity yet today)"
    - "App deployed to Vercel and accessible via URL"
    - "App renders correctly on tablet without pinch-zoom"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Manager dashboard page"
      min_lines: 20
    - path: "src/app/api/dashboard/route.ts"
      provides: "API route reading Manager-View tab from Google Sheets"
      exports: ["GET"]
    - path: "src/components/ManagerDashboard.tsx"
      provides: "Dashboard UI rendering location/shift status grid"
  key_links:
    - from: "src/app/api/dashboard/route.ts"
      to: "src/lib/sheets.ts"
      via: "readTab('Manager-View', ...)"
      pattern: "readTab.*Manager-View"
    - from: "src/components/ManagerDashboard.tsx"
      to: "/api/dashboard"
      via: "fetch on mount"
      pattern: "fetch.*api/dashboard"

user_setup:
  - service: google-sheets
    why: "Checklist completions data store"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_KEY
        source: "Base64-encode your service account JSON key: `base64 -i service-account-key.json | tr -d '\\n'`"
      - name: CHECKLIST_SHEET_ID
        source: "Create a new Google Sheet, share with service account email as Editor, copy the Sheet ID from the URL"
    dashboard_config:
      - task: "Create Google Sheet with 3 tabs: Completions-Log (headers in A1:H1), Checklist-Template (optional), Manager-View (QUERY formula)"
        location: "Google Sheets"
      - task: "Share Sheet with service account email as Editor"
        location: "Google Sheets sharing settings"
  - service: vercel
    why: "App hosting"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_KEY
        source: "Same base64 value as local .env.local"
      - name: CHECKLIST_SHEET_ID
        source: "Same Sheet ID as local .env.local"
---

<objective>
Build the manager dashboard that reads pre-computed data from the Manager-View Sheet tab, deploy the complete app to Vercel, and verify the full flow on a tablet-like screen.

Purpose: Managers (Rez and ops partner) need to see shift completion status across all 5 locations at a glance from their phone. Plus the app needs to be deployed and accessible.

Output: Working /dashboard route showing today's checklist status, app deployed to Vercel, human-verified on tablet viewport.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-daily-checklist-app/03-CONTEXT.md
@.planning/phases/03-daily-checklist-app/03-RESEARCH.md
@.planning/phases/03-daily-checklist-app/03-01-SUMMARY.md
@.planning/phases/03-daily-checklist-app/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 consumed by this plan -->

From src/lib/sheets.ts:
```typescript
export function readTab(tabName: string, range: string): Promise<string[][]>;
```

From src/lib/types.ts:
```typescript
export const LOCATIONS = ['Moto Medi Lubbock 1', 'Moto Medi Lubbock 2', 'Moto Medi Amarillo', 'Tikka Shack 1', 'Tikka Shack 2'] as const;
export type ShiftType = 'Opening' | 'Closing';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build manager dashboard API route and UI</name>
  <files>
    /Users/josi/rez-checklist/src/app/api/dashboard/route.ts
    /Users/josi/rez-checklist/src/app/dashboard/page.tsx
    /Users/josi/rez-checklist/src/components/ManagerDashboard.tsx
  </files>
  <action>
    1. Create `src/app/api/dashboard/route.ts`:
       - Import readTab from lib/sheets.ts
       - GET handler that reads the Manager-View tab: readTab('Manager-View', 'A1:Z100')
       - Parse the QUERY-computed rows into a structured response:
         ```typescript
         interface LocationShiftStatus {
           location: string;
           shift: string;
           staffName: string;
           itemsCompleted: number;
         }
         ```
       - Group by location and shift to produce a summary per location:
         - For each of the 5 LOCATIONS, for each shift (Opening/Closing):
           - status: 'complete' (all items done), 'in-progress' (some items done), 'not-started' (zero items)
           - staffName (who started the shift)
           - itemsCompleted count
       - Note: determining "complete" requires knowing total items per location/shift. Import getItemsForSession from checklist-config.ts to get the expected count.
       - Handle empty Manager-View tab gracefully: return all locations as 'not-started'
       - Return JSON: { date: today's date string, locations: LocationShiftStatus[] }
       - Set Cache-Control: no-store (always fresh data for managers)
       - Wrap in try/catch: on Sheets API error, return 500 with { error: 'Failed to load dashboard data' }

    2. Create `src/components/ManagerDashboard.tsx` ('use client'):
       - Fetches /api/dashboard on mount (useEffect + fetch)
       - Loading state: skeleton cards or spinner
       - Error state: "Unable to load dashboard. Try refreshing."
       - Empty state: "No activity yet today" with current date
       - Main view: grid of location cards (responsive: 1 col on phone, 2-3 cols on tablet)
       - Each location card shows:
         - Location name (bold header)
         - Opening shift row: status badge (green "Complete" / amber "In Progress 3/8" / gray "Not Started"), staff name if started
         - Closing shift row: same
         - Visual weight on incomplete shifts -- draw the eye to what needs attention
       - Status badges: green filled for complete, amber/yellow for in-progress with count, gray/muted for not-started
       - Auto-refresh: refetch every 60 seconds (setInterval in useEffect) so managers see updates without manual refresh
       - Group Moto Medi locations and Tikka Shack locations visually (matching the brand grouping from the reporting workbook)

    3. Create `src/app/dashboard/page.tsx`:
       - Simple page wrapper rendering ManagerDashboard component
       - Page title: "Manager Dashboard" with today's date
       - No session context needed -- managers don't go through session-start flow
       - Include a subtle link back to "/" (for if someone navigates here from the tablet checklist by accident)
       - Mobile-scannable: Rez checks this on his phone between locations, so it must be readable on a phone too, not just tablet

    AESTHETICS:
    - Dashboard should feel like a command center -- dense but readable
    - Location cards: clean borders, subtle shadows, clear hierarchy
    - Status badges should be instantly scannable (color-first, then text)
    - Consider using the brand aesthetic: Moto Medi cards with one accent color, Tikka Shack with another
    - The "all complete" state for a location should feel satisfying -- full green card
    - Mobile-friendly: stack cards vertically on narrow screens
    - Distinctive from the staff checklist -- slightly different feel (more data-dense, less action-oriented)
  </action>
  <verify>
    cd /Users/josi/rez-checklist && npm run build
    Build exits 0. `grep -r "Manager-View" src/app/api/dashboard/` confirms reading the pre-computed tab. `grep -r "no-store" src/app/api/dashboard/` confirms cache control. `grep -r "getItemsForSession" src/app/api/dashboard/` confirms total count lookup.
  </verify>
  <done>
    /dashboard page renders location cards with shift status for all 5 locations. API route reads Manager-View tab from Sheets. Empty state handled gracefully. Auto-refreshes every 60s. Mobile-scannable layout. Build passes clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize git repo, prepare Google Sheet structure, and deploy to Vercel</name>
  <files>
    /Users/josi/rez-checklist/.gitignore
  </files>
  <action>
    1. Initialize git in /Users/josi/rez-checklist/:
       - `git init`
       - Verify .gitignore includes: node_modules, .next, .env.local, .env*.local
       - `git add -A && git commit -m "feat: rez checklist app — tablet shift checklist with Sheets backend"`

    2. Provide instructions for Google Sheet setup (output to terminal, do NOT automate -- human must create Sheet):
       Print clear instructions:
       - Create a new Google Sheet named "Rez Shift Checklists"
       - Tab 1: "Completions-Log" with headers in row 1: Timestamp | Location | Shift | StaffName | ItemID | ItemText | Category | Notes
       - Tab 2: "Manager-View" with this QUERY formula in A1:
         ```
         =IFERROR(QUERY('Completions-Log'!A:H, "SELECT B, C, D, COUNT(E) WHERE A >= date '"&TEXT(TODAY(),"yyyy-mm-dd")&"' GROUP BY B, C, D LABEL COUNT(E) 'ItemsCompleted'", 1), {"No data today"})
         ```
       - Share the Sheet with the service account email as Editor
       - Copy the Sheet ID from the URL (the long string between /d/ and /edit)
       - NOTE about QUERY date filtering: If ISO timestamps don't work with the QUERY date filter, add a helper column (e.g., column I) with formula `=IF(A2="","",DATEVALUE(LEFT(A2,10)))` and update the QUERY to filter on column I instead.

    3. Deploy to Vercel:
       - Run `npx vercel --yes` (or `vercel --yes` if available globally)
       - If prompted for project setup, accept defaults (Next.js, /Users/josi/rez-checklist)
       - Set env vars: `npx vercel env add GOOGLE_SERVICE_ACCOUNT_KEY` and `npx vercel env add CHECKLIST_SHEET_ID`
       - Note: if Vercel CLI not installed, run `npm i -g vercel` first
       - After env vars set: `npx vercel --prod`
       - Record the deployed URL

    4. After deploy, print the deployed URL and instructions for setting up the tablet bookmark.
  </action>
  <verify>
    - `cd /Users/josi/rez-checklist && npm run build` exits 0
    - `git log --oneline` shows at least one commit
    - Vercel deployment URL is accessible (curl returns 200)
  </verify>
  <done>
    Git repo initialized with clean commit. Google Sheet setup instructions printed. App deployed to Vercel with env vars configured. Deployed URL returned and accessible.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Rez Checklist app: session-start flow (location + shift + name), categorized checklist with tap-to-complete and notes, manager dashboard showing all-location status, deployed to Vercel.
  </what-built>
  <how-to-verify>
    1. Open the Vercel URL on your phone or tablet (or use browser dev tools tablet emulation)
    2. Verify session-start screen:
       - All 5 locations visible as large buttons
       - Tap a location, then Opening or Closing
       - Enter a name, tap Start Checklist
    3. Verify checklist page:
       - Items grouped by category (Opening Procedures, Cleaning, Equipment, etc.)
       - Tap an item -- should show checkmark immediately
       - Expand notes on an item, type a note, then complete it
       - Header shows your location, shift, and name
    4. Verify data flow:
       - Open the Google Sheet "Rez Shift Checklists" -> Completions-Log tab
       - Confirm completed items appear as rows with timestamp, location, shift, name, item details
    5. Verify manager dashboard:
       - Navigate to /dashboard
       - Should show all 5 locations with shift status
       - The shift you just completed should show as "In Progress" with your name
    6. Verify ghost session protection:
       - Refresh the page -- should return to session-start screen (not the checklist)
    7. Verify tablet feel:
       - No pinch-zoom allowed
       - All buttons large enough to tap easily
       - Readable without squinting
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- /dashboard API route reads Manager-View tab (not raw Completions-Log)
- Dashboard handles empty state (no activity today)
- App deployed to Vercel with correct env vars
- Full staff flow works: session start -> checklist -> completion -> Sheets row
- Manager dashboard shows status across all locations
- Page refresh returns to session-start (no ghost sessions)
- Tablet viewport locked (no pinch-zoom)
- Human verification checkpoint passed
</verification>

<success_criteria>
The complete Rez Checklist app is deployed and functional: staff can complete shift checklists on a shared tablet, completions write to Google Sheets, managers can see status across all 5 locations. Human verification confirms the app works as intended on a tablet-sized screen.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-checklist-app/03-03-SUMMARY.md`
</output>
