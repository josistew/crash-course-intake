---
phase: 01-weekly-reporting
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/build_weekly_report.py
autonomous: true
requirements: [RPT-06, RPT-07, RPT-08, RPT-09]

must_haves:
  truths:
    - "Each location calc tab shows 5 KPIs computed from import tab data using MATCH-based formulas"
    - "Summary tab shows all 5 locations side-by-side with computed KPIs and WoW variance"
    - "WoW variance cells show arrow + percentage format and are conditionally formatted red/green for >5% swings"
    - "Prior-Week tab is referenced by date-keyed lookup, not row offset"
    - "Formulas produce correct non-zero values when sample data is present"
  artifacts:
    - path: "scripts/build_weekly_report.py"
      provides: "Updated script with all formulas for calc tabs, summary, and WoW comparison"
      min_lines: 400
    - path: "Rez-Weekly-Report.xlsx"
      provides: "Complete workbook ready for Rez to upload to Google Drive"
  key_links:
    - from: "MML-Calc (and other calc tabs)"
      to: "Square-Import, DoorDash-Import, UberEats-Import, Grubhub-Import, BEK-Import"
      via: "SUMIFS with MATCH-based column references"
      pattern: "SUMIFS.*MATCH"
    - from: "Summary"
      to: "MML-Calc through TS2-Calc"
      via: "Direct cell references to calc tab KPI cells"
      pattern: "='MML-Calc'!"
    - from: "Summary WoW cells"
      to: "Prior-Week"
      via: "MATCH on week ending date for date-keyed lookup"
      pattern: "MATCH.*Prior-Week"
---

<objective>
Add all formulas to the workbook: location calc tabs get MATCH-based SUMIFS formulas pulling from import tabs, Summary tab gets cross-tab references with WoW variance display, and conditional formatting highlights >5% swings in red/green. This completes the formula engine.

Purpose: Turns the structural skeleton from Plan 01 into a working reporting tool. After this, Rez can paste real CSVs and see metrics auto-populate.
Output: Updated `scripts/build_weekly_report.py` producing a complete `Rez-Weekly-Report.xlsx`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-weekly-reporting/01-CONTEXT.md
@.planning/phases/01-weekly-reporting/01-RESEARCH.md
@.planning/phases/01-weekly-reporting/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MATCH-based SUMIFS formulas to all 5 location calc tabs</name>
  <files>scripts/build_weekly_report.py</files>
  <action>
Update `build_weekly_report.py` to add formulas to each location calc tab (MML-Calc, MMA-Calc, MM3-Calc, TS1-Calc, TS2-Calc). Use a loop over locations — each calc tab gets the same formula structure with the location ID substituted.

**IMPORTANT openpyxl formula note:** openpyxl writes Excel formula syntax. When Rez uploads to Google Sheets, Excel formulas auto-convert. Use Excel-compatible functions only: SUMIFS, MATCH, INDEX, IF, IFERROR, ISNUMBER, VLOOKUP, SUMPRODUCT. Do NOT use Google-only functions (QUERY, ARRAYFORMULA, FILTER).

For each location calc tab, the location's short ID (e.g., "Moto Medi Lubbock" for MML) is used in SUMIFS criteria. The script must map each location ID to the exact string that appears in CSV Location columns.

**B2 (Week Ending):** Link to Summary!B2 — `='Summary'!B2`

**Revenue formulas (column B, "This Week"):**

Row 5 — Square Net Sales:
```
=IFERROR(SUMIFS(
  INDEX('Square-Import'!A:Z,0,MATCH("Net Sales",'Square-Import'!1:1,0)),
  INDEX('Square-Import'!A:Z,0,MATCH("Location",'Square-Import'!1:1,0)),
  "{location_full_name}"
), 0)
```

Row 6 — DoorDash Net Revenue:
```
=IFERROR(SUMIFS(
  INDEX('DoorDash-Import'!A:Z,0,MATCH("Net Payout",'DoorDash-Import'!1:1,0)),
  INDEX('DoorDash-Import'!A:Z,0,MATCH("Store Name",'DoorDash-Import'!1:1,0)),
  "{location_full_name}"
), 0)
```

Row 7 — UberEats Net Revenue (same pattern, "Restaurant Name" and "Net Payout" columns)

Row 8 — Grubhub Net Revenue (same pattern, "Restaurant" and "Net Payout" columns)

Row 9 — Total Net Revenue: `=SUM(B5:B8)`

**Cost formulas:**

Row 12 — BEK Purchases:
```
=IFERROR(SUMIFS(
  INDEX('BEK-Import'!A:Z,0,MATCH("Total",'BEK-Import'!1:1,0)),
  INDEX('BEK-Import'!A:Z,0,MATCH("Category",'BEK-Import'!1:1,0)),
  "Food"
), SUMIF(
  INDEX('BEK-Import'!A:Z,0,MATCH("Invoice Date",'BEK-Import'!1:1,0)),
  ">0",
  INDEX('BEK-Import'!A:Z,0,MATCH("Total",'BEK-Import'!1:1,0))
))
```
Note: Falls back to summing ALL BEK items if "Category" column doesn't exist (MATCH returns error). Add a cell comment: "If BEK has no Category column, this sums all purchases. Label as 'BEK Total Purchases %' in that case."

The BEK import is not location-specific in the current design (BEK delivers to all locations combined). For v1, divide BEK total evenly across locations OR sum all to the first location. Add a comment: "BEK purchases are company-wide. Divide by number of locations for per-location estimate, or allocate manually."

Simplified approach: `=IFERROR(SUMIF(INDEX('BEK-Import'!A:Z,0,MATCH("Total",'BEK-Import'!1:1,0)),"<>",INDEX('BEK-Import'!A:Z,0,MATCH("Total",'BEK-Import'!1:1,0)))/5, 0)` — divides total by 5 locations.

Row 13 — Purchase Cost %: `=IF(B9>0, B12/B9, 0)` — format as percentage

**Labor formulas:**

Row 16 — Estimated Labor Cost:
```
=IFERROR(SUMPRODUCT(
  (INDEX('Square-Import'!A:Z,0,MATCH("Location",'Square-Import'!1:1,0))="{location_full_name}")*
  INDEX('Square-Import'!A:Z,0,MATCH("Total Hours",'Square-Import'!1:1,0))*
  IFERROR(VLOOKUP(INDEX('Square-Import'!A:Z,0,MATCH("Employee Name",'Square-Import'!1:1,0)),'Employee-Roster'!B:D,3,FALSE),0)
), 0)
```

Wait — the labor data comes from a SEPARATE Square Labor export, not the Sales Summary. But we only have one Square-Import tab. Per the research, Square has different export types (Sales Summary vs Labor). We should note this limitation.

REVISED approach: Since we have one Square-Import tab designed for Sales Summary, labor cost calculation needs the Employee-Roster tab's hourly rates but we don't have a separate labor hours import. For v1, add a simple approach:

Row 16: `=SUMPRODUCT(('Employee-Roster'!C:C="{location_id}")*('Employee-Roster'!D:D))` — this just sums pay rates, which isn't useful.

BETTER: Skip the SUMPRODUCT formula for labor in v1. Instead:
- Row 16: Manual entry cell with note "Enter total labor hours for this location this week. Multiply by avg hourly rate from Employee Roster."
- Or: Simple formula `=B16_hours * AVERAGEIFS('Employee-Roster'!D:D, 'Employee-Roster'!C:C, "{location_id}")`
- Add a row 15.5 for "Total Hours" (manual entry or from Square Labor — to be determined in Phase 2)

SIMPLEST v1 approach that works:
- Row 16 label: "Est. Labor Cost (manual)" — leave cell B16 for manual entry with note "Enter from Square labor report: total hours x avg rate for this location"
- Row 17: `=IF(B9>0, B16/B9, 0)` — Labor Cost % from manual labor cost

This is honest — labor cost requires Square Labor CSV which is a separate export. Phase 2 handles this properly with PAY-01.

**Volume formulas:**

Row 20 — Total Orders:
```
=IFERROR(SUMIFS(
  INDEX('Square-Import'!A:Z,0,MATCH("Order Count",'Square-Import'!1:1,0)),
  INDEX('Square-Import'!A:Z,0,MATCH("Location",'Square-Import'!1:1,0)),
  "{location_full_name}"
), 0)
+ IFERROR(SUMIFS(
  INDEX('DoorDash-Import'!A:Z,0,MATCH("Order Count",'DoorDash-Import'!1:1,0)),
  INDEX('DoorDash-Import'!A:Z,0,MATCH("Store Name",'DoorDash-Import'!1:1,0)),
  "{location_full_name}"
), 0)
+ IFERROR(SUMIFS(
  INDEX('UberEats-Import'!A:Z,0,MATCH("Trips",'UberEats-Import'!1:1,0)),
  INDEX('UberEats-Import'!A:Z,0,MATCH("Restaurant Name",'UberEats-Import'!1:1,0)),
  "{location_full_name}"
), 0)
+ IFERROR(SUMIFS(
  INDEX('Grubhub-Import'!A:Z,0,MATCH("Orders",'Grubhub-Import'!1:1,0)),
  INDEX('Grubhub-Import'!A:Z,0,MATCH("Restaurant",'Grubhub-Import'!1:1,0)),
  "{location_full_name}"
), 0)
```

Row 21 — Avg Ticket: `=IF(B20>0, B9/B20, 0)` — format as currency

**Prior Week column (column C):**
For each KPI row, reference the Prior-Week tab using INDEX/MATCH on the week ending date:
```
=IFERROR(INDEX('Prior-Week'!B:Z, MATCH(B2-7,'Prior-Week'!A:A,0), {column_offset}), 0)
```
Where `{column_offset}` maps to the correct Prior-Week column for this location's metric (computed from the Prior-Week header layout: each location has 5 metric columns).

**WoW Change column (column D):** `=B5-C5` (absolute change)

**WoW % column (column E):** `=IF(C5>0, (B5-C5)/C5, 0)` — format as percentage with +/- sign

**WoW display formatting:** Use conditional formatting on column E:
- Green fill (D9EAD3) + dark green font (006100) if value > 0.05
- Red fill (FBE4E4) + dark red font (9C0006) if value < -0.05
- No special formatting for values between -5% and +5%

Apply all these formulas in a loop over the 5 locations, substituting location ID, full name, and Prior-Week column offset.
  </action>
  <verify>
    <automated>cd /Users/josi/crash-course-intake && python3 scripts/build_weekly_report.py && python3 -c "
import openpyxl
wb = openpyxl.load_workbook('Rez-Weekly-Report.xlsx')
# Check each calc tab has formulas in column B
for loc in ['MML', 'MMA', 'MM3', 'TS1', 'TS2']:
    ws = wb[f'{loc}-Calc']
    # Check Net Revenue formula exists (row 9 should be SUM)
    val = ws.cell(9, 2).value
    assert val is not None, f'{loc}-Calc row 9 col B is empty (should have Total Net Revenue formula)'
    # Check Purchase Cost % formula exists
    val13 = ws.cell(13, 2).value
    assert val13 is not None, f'{loc}-Calc row 13 col B is empty (should have Purchase Cost %)'
    # Check WoW % column has formulas
    val_e = ws.cell(5, 5).value
    assert val_e is not None, f'{loc}-Calc row 5 col E is empty (should have WoW %)'
print('ALL CALC TAB FORMULA CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - All 5 location calc tabs have MATCH-based SUMIFS formulas pulling from import tabs
    - Revenue breakdown shows Square + 3 delivery platforms summed to Total Net Revenue
    - Purchase Cost % computed from BEK imports divided by net revenue
    - Labor Cost % cell exists (manual entry for v1, formula-ready for Phase 2)
    - Order volume aggregated across all platforms
    - Avg ticket = net revenue / total orders
    - Prior Week column references Prior-Week tab via date-keyed MATCH
    - WoW % column has conditional formatting (red/green for >5% swings)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Summary tab formulas with cross-tab references, WoW display, and conditional formatting</name>
  <files>scripts/build_weekly_report.py</files>
  <action>
Update `build_weekly_report.py` to populate the Summary tab with formulas that reference each location calc tab.

**Summary tab formula layout** (locations as columns B-F, metrics as rows):

Row 2, B2: Week ending date — manual entry cell with date format and note "Enter the Sunday date for this reporting week (e.g., 2026-03-15)"

For each location column (B=MML, C=MMA, D=MM3, E=TS1, F=TS2):

Row 5 (Net Revenue): `='MML-Calc'!B9` (references Total Net Revenue from calc tab) — format as currency, no decimals
Row 6 (WoW): `=IF('MML-Calc'!C9>0, IF('MML-Calc'!E5>=0, CHAR(8593)&" "&TEXT('MML-Calc'!E5,"+0.0%"), CHAR(8595)&" "&TEXT(ABS('MML-Calc'!E5),"0.0%")), "—")` — displays as "↑ +3.2%" or "↓ -7.1%" using Unicode arrows

Wait — the WoW % is on different rows per metric in the calc tab. Let me map it:
- Net Revenue is row 9 in calc, WoW % is row 9 col E... No, the calc tab layout has each metric on its own row with WoW in col E of THAT row.

Actually re-reading Plan 01's calc tab layout:
- Row 5: Square Net Sales
- Row 9: Total Net Revenue
- Row 13: Purchase Cost %
- Row 17: Labor Cost %
- Row 20: Total Orders
- Row 21: Avg Ticket

So the Summary references:
- Net Revenue → `='{loc}-Calc'!B9` (Total Net Revenue)
- WoW for Net Revenue → reference `'{loc}-Calc'!E9` (WoW % for Total Net Revenue)
- Purchase Cost % → `='{loc}-Calc'!B13`
- WoW for Purchase Cost % → `='{loc}-Calc'!E13`
- Labor Cost % → `='{loc}-Calc'!B17`
- WoW for Labor Cost % → `='{loc}-Calc'!E17`
- Order Volume → `='{loc}-Calc'!B20`
- WoW for Order Volume → `='{loc}-Calc'!E20`
- Avg Ticket → `='{loc}-Calc'!B21`
- WoW for Avg Ticket → `='{loc}-Calc'!E21`

**WoW display formula** (for each WoW row):
```
=IF({loc}-Calc!C{row}>0,
  IF({loc}-Calc!E{row}>=0,
    "↑ "&TEXT({loc}-Calc!E{row},"+0.0%"),
    "↓ "&TEXT(ABS({loc}-Calc!E{row}),"0.0%")
  ),
  "—"
)
```

Use CHAR(8593) for ↑ and CHAR(8595) for ↓ in Excel formula syntax (openpyxl may need the actual Unicode characters in the formula string — test both approaches).

**Conditional formatting on WoW rows (rows 6, 8, 10, 12, 14):**
Cannot easily do this with openpyxl conditional formatting on formula-produced text strings. Instead, add a HIDDEN column (column H onward) with the raw WoW % values, and apply conditional formatting to the visible WoW cells based on those hidden values.

SIMPLER approach: Apply conditional formatting directly to the WoW display cells using a formula rule:
- Rule: `='{loc}-Calc'!E{row}>0.05` → green fill
- Rule: `='{loc}-Calc'!E{row}<-0.05` → red fill

Use openpyxl's `FormulaRule` or `CellIsRule` for conditional formatting.

**Actually simplest approach:** Put the raw WoW % value in the WoW rows (not the arrow string), format as percentage with +/- sign, and let conditional formatting work directly on the numeric value. The arrow display is nice but adds complexity for v1. The research says "arrow + percentage next to each metric" but a colored percentage cell communicates the same information.

USE THIS: WoW rows contain the raw percentage: `='{loc}-Calc'!E{row}` formatted as "+0.0%;-0.0%" with conditional formatting for >5% / <-5%.

**Brand color banding:**
- Columns B-D (Moto Medi): light warm background on header row (FDE9D9)
- Columns E-F (Tikka Shack): light cool background on header row (D0E2F1)
- Apply to row 4 (column headers) and extend as subtle alternating bands

**Row formatting:**
- Metric rows (5, 7, 9, 11, 13): bold labels, currency/percentage format on values
- WoW rows (6, 8, 10, 12, 14): smaller font (9pt), indented label, percentage format
- Add thin bottom borders between metric groups

**Additional Summary elements:**
- Row 16: blank separator
- Row 17: "Notes" section header
- Row 18: "Purchase Cost % = BEK purchases / net revenue. Not true COGS (requires inventory counts)."
- Row 19: "Labor Cost % is estimated from manual entry. Phase 2 automates via Square Labor import."
- Row 20: "All delivery revenue uses net payout (after platform fees/commissions)."

**Final script updates:**
- Add a `--output` CLI argument to specify output filename (default: "Rez-Weekly-Report.xlsx")
- Add a print statement showing the output path and tab count
- Ensure the script is idempotent (running twice overwrites cleanly)

After all formulas are added, run the script and verify the output file.
  </action>
  <verify>
    <automated>cd /Users/josi/crash-course-intake && python3 scripts/build_weekly_report.py && python3 -c "
import openpyxl
wb = openpyxl.load_workbook('Rez-Weekly-Report.xlsx')
ws = wb['Summary']
# Check Summary has location headers
assert ws.cell(4, 2).value is not None, 'Summary missing MML column header'
assert ws.cell(4, 6).value is not None, 'Summary missing TS2 column header'
# Check Net Revenue row has formulas/values
for col in range(2, 7):
    val = ws.cell(5, col).value
    assert val is not None, f'Summary row 5 col {col} is empty (Net Revenue)'
# Check WoW rows have formulas/values
for col in range(2, 7):
    val = ws.cell(6, col).value
    assert val is not None, f'Summary row 6 col {col} is empty (WoW)'
# Check conditional formatting exists on Summary
assert len(ws.conditional_formatting._cf_rules) > 0, 'No conditional formatting on Summary tab'
print('ALL SUMMARY CHECKS PASSED')
# Final count
print(f'Workbook has {len(wb.sheetnames)} tabs: {wb.sheetnames}')
print(f'Summary has data through row {ws.max_row}')
"
    </automated>
  </verify>
  <done>
    - Summary tab shows all 5 locations as columns with brand color banding (Moto Medi warm, Tikka Shack cool)
    - Each KPI row pulls from the corresponding location calc tab
    - WoW rows show percentage change with conditional formatting: green >+5%, red <-5%
    - Week ending date cell drives all date-dependent formulas
    - Notes section explains metric definitions
    - Complete .xlsx file ready for Rez to upload to Google Drive
    - Script runs cleanly with informative output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Rez Weekly Report workbook generated as .xlsx file with 14 tabs: Instructions, 5 Import tabs (with placeholder headers and sample data), Employee Roster, Prior Week, 5 Location Calc tabs (with MATCH-based formulas), and Summary tab (with cross-tab references and WoW variance formatting).</what-built>
  <how-to-verify>
    1. Open `Rez-Weekly-Report.xlsx` in Google Sheets (upload to Google Drive > Open with Google Sheets)
    2. Check the Instructions tab — should have clear step-by-step guides per platform
    3. Check any Import tab (e.g., Square-Import) — should have column headers, a validation row, and sample data below
    4. Check a Location Calc tab (e.g., MML-Calc) — should show Net Revenue, Purchase Cost %, Order Volume, Avg Ticket with formula-computed values from sample data
    5. Check the Summary tab — should show all 5 locations as columns with KPI values and WoW percentages. Moto Medi columns should have warm background, Tikka Shack cool background.
    6. Verify WoW cells with >5% change show green (positive) or red (negative) conditional formatting
    7. Check that the week ending date in Summary B2 drives the Prior-Week lookup
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the layout, formulas, or formatting</resume-signal>
</task>

</tasks>

<verification>
- `python3 scripts/build_weekly_report.py` produces Rez-Weekly-Report.xlsx without errors
- All 14 tabs present with correct structure and formulas
- Import tabs use MATCH-based column references (never hardcoded column letters)
- Location calc tabs compute all KPIs from import tab data
- Summary tab consolidates all 5 locations with WoW variance formatting
- Conditional formatting highlights >5% swings in red/green
- File opens correctly in Google Sheets after upload
</verification>

<success_criteria>
Rez can upload Rez-Weekly-Report.xlsx to Google Drive, open in Google Sheets, paste real CSVs into import tabs, and see all metrics auto-populate on the Summary tab — with WoW comparison working against Prior-Week data. The workbook is self-documenting via the Instructions tab.
</success_criteria>

<output>
After completion, create `.planning/phases/01-weekly-reporting/01-02-SUMMARY.md`
</output>
