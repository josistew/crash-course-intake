---
phase: 01-weekly-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build_weekly_report.py
  - scripts/sample_data.py
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-05, RPT-10]

must_haves:
  truths:
    - "Python script runs and produces a valid .xlsx file without errors"
    - "Opening the .xlsx shows 14 tabs in correct order: Instructions, 5 Import tabs, Employee-Roster, Prior-Week, 5 Location Calc tabs, Summary"
    - "Each import tab has a header row with placeholder column names and a validation row that checks ISNUMBER on key columns"
    - "Instructions tab contains step-by-step paste instructions for each platform"
    - "All import tab formulas use MATCH-based column lookups, never hardcoded column letters"
  artifacts:
    - path: "scripts/build_weekly_report.py"
      provides: "Main Python script that generates the .xlsx workbook"
      min_lines: 200
    - path: "scripts/sample_data.py"
      provides: "Sample CSV-like data for each platform to test formulas"
      min_lines: 50
  key_links:
    - from: "scripts/build_weekly_report.py"
      to: "output .xlsx file"
      via: "openpyxl workbook.save()"
      pattern: "workbook\\.save"
    - from: "scripts/sample_data.py"
      to: "scripts/build_weekly_report.py"
      via: "import for test data population"
      pattern: "from sample_data import"
---

<objective>
Build the Python/openpyxl script that generates the complete weekly reporting workbook structure: all 14 tabs, import tab layouts with placeholder column headers and ISNUMBER validation rows, Instructions tab content, Employee Roster structure, and sample data population for testing.

Purpose: Creates the data input foundation — every tab that receives pasted CSV data or stores reference data. Plan 02 builds the calculation and summary layer on top of this.
Output: `scripts/build_weekly_report.py` that produces `Rez-Weekly-Report.xlsx`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-weekly-reporting/01-CONTEXT.md
@.planning/phases/01-weekly-reporting/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workbook scaffold with all 14 tabs, import tab layouts, and sample data module</name>
  <files>scripts/build_weekly_report.py, scripts/sample_data.py</files>
  <action>
Create `scripts/` directory. Build two Python files:

**scripts/sample_data.py** — Module containing sample/placeholder data for each platform:
- `SQUARE_HEADERS`: list of likely Square Sales Summary CSV columns: ["Date", "Location", "Category", "Net Sales", "Gross Sales", "Discounts", "Tax", "Tips", "Refunds"]. These are PLACEHOLDERS — Rez must validate exact column names from his actual exports.
- `DOORDASH_HEADERS`: ["Date", "Store Name", "Subtotal", "Commission", "Net Payout", "Order Count", "Avg Order Value"]
- `UBEREATS_HEADERS`: ["Date", "Restaurant Name", "Sales", "Marketplace Fee", "Net Payout", "Trips", "Avg Basket"]
- `GRUBHUB_HEADERS`: ["Date", "Restaurant", "Food Sales", "Delivery Fee", "Grubhub Commission", "Net Payout", "Orders"]
- `BEK_HEADERS`: ["Invoice Date", "Invoice Number", "Item Description", "Category", "Quantity", "Unit Price", "Total"]
- `SQUARE_LABOR_HEADERS`: ["Date", "Employee Name", "Location", "Clock In", "Clock Out", "Regular Hours", "Overtime Hours", "Total Hours"]
- `LOCATIONS`: dict mapping short IDs to full names: {"MML": "Moto Medi Lubbock", "MMA": "Moto Medi Amarillo", "MM3": "Moto Medi 3rd", "TS1": "Tikka Shack 1", "TS2": "Tikka Shack 2"}
- `BRANDS`: {"Moto Medi": ["MML", "MMA", "MM3"], "Tikka Shack": ["TS1", "TS2"]}
- Sample data rows for each platform (5-10 rows per platform with realistic values across multiple locations) for testing formulas. Use week of 2026-03-09 to 2026-03-15.
- Sample employee roster: 8-10 employees spread across locations with hourly rates $12-$18.

**scripts/build_weekly_report.py** — Main script using openpyxl:

1. **Create workbook** with these tabs in order (delete default "Sheet"):
   - Instructions
   - Square-Import
   - DoorDash-Import
   - UberEats-Import
   - Grubhub-Import
   - BEK-Import
   - Employee-Roster
   - Prior-Week
   - MML-Calc, MMA-Calc, MM3-Calc, TS1-Calc, TS2-Calc
   - Summary

2. **Tab colors** (using openpyxl tab_color):
   - Import tabs: "4A86C8" (steel blue)
   - Calc tabs: "999999" (neutral gray)
   - Summary + Prior-Week: "F4A442" (amber — the tabs Rez actually uses)
   - Instructions + Employee-Roster: "6AA84F" (sage green — reference tabs)

3. **Instructions tab** (locked per CONTEXT.md decision):
   - Title row: "Rez Weekly Report — Instructions" in bold, 16pt
   - Section per platform with exact navigation path:
     - Square: "Reports > Sales Summary > Select date range (Mon-Sun) > Export CSV > Paste into Square-Import tab"
     - DoorDash: "Merchant Portal > Financials > Report Builder > Select week > Download CSV > Paste into DoorDash-Import tab"
     - UberEats: "Uber Eats Manager > Analytics > Download Report > Select week > Paste into UberEats-Import tab"
     - Grubhub: "Restaurant Hub > Reports > Select week > Export > Paste into Grubhub-Import tab"
     - BEK Entree: "BEK Entree Portal > Order History > Export Invoice CSV > Paste into BEK-Import tab"
   - "Weekly Cadence" section: "Every Monday morning: clear last week's data from each import tab, paste new CSVs, check Summary tab"
   - "Troubleshooting" section: "If you see $0 or ERROR in the Summary tab, check the validation row (row 2) in each import tab — it will tell you which column had a paste issue"
   - Note about placeholder column names: "IMPORTANT: Column headers in import tabs are placeholders. After your first CSV export from each platform, update the header row in each import tab to match your actual column names. The formulas use MATCH() on these headers — they must match exactly."
   - Freeze pane at row 1
   - Column A width: 80 characters

4. **Each Import tab** (Square, DoorDash, UberEats, Grubhub, BEK):
   - Row 1: Column headers from sample_data (bold, frozen)
   - Row 2: Validation row — for each column that should be numeric, place a formula comment/note: "Validation: paste data starting row 3. Check this row shows OK after pasting." Use openpyxl comments.
   - Row 2 formula logic (as cell values since openpyxl can write Excel formulas):
     For key numeric columns (Net Sales, Net Payout, Total, etc.):
     `=IF(ISNUMBER(A3),"OK","PASTE ERROR - not a number")` adjusted per column
   - Row 2 cells: light red fill (FBE4E4) as default — will turn to light green when validation passes
   - Conditional formatting: row 2 cells turn green fill (D9EAD3) when value = "OK"
   - Row 3+: Empty (paste zone) — but populate with sample data from sample_data.py for testing
   - "Last Updated" cell in cell A(last used row + 2): label "Last Updated:" in col A, empty date cell in col B with a note "Enter the date you pasted this data"
   - Column widths: auto-sized to header text length + padding
   - Add a note at top of each import tab header: "PLACEHOLDER HEADERS — Update to match your actual CSV export column names"

5. **Employee-Roster tab**:
   - Headers: Employee Name | Square Name | Location | Hourly Rate | Hire Date | Pay Tier | Notes
   - "Square Name" column is the VLOOKUP key (per research — handles nickname mismatches)
   - Populate with sample employees from sample_data.py
   - Data validation on Location column: dropdown list of location IDs (MML, MMA, MM3, TS1, TS2)
   - Column formatting: Hire Date as date, Hourly Rate as currency
   - Freeze row 1

6. **Prior-Week tab**:
   - Row 1 headers: Week Ending | then for each location (MML, MMA, MM3, TS1, TS2): Net Revenue, Purchase Cost %, Labor Cost %, Order Volume, Avg Ticket (25 metric columns + date = 26 columns total)
   - Row 2: sample prior week data (week ending 2026-03-09) with realistic values
   - Row 3: empty (current week — will be populated by formulas or manual copy from Summary)
   - Freeze row 1
   - Column A formatted as date

7. **Location Calc tabs** (MML-Calc through TS2-Calc) — STRUCTURE ONLY in this task:
   - Row 1: Location full name (from LOCATIONS dict) in bold, merged across columns A-F
   - Row 2: "Week Ending:" label in A2, empty cell B2 (will be linked to a master date cell)
   - Row 4: Section header "Revenue Breakdown" in bold
   - Row 5-9: Labels in column A: "Square Net Sales", "DoorDash Net Revenue", "UberEats Net Revenue", "Grubhub Net Revenue", "Total Net Revenue"
   - Row 11: Section header "Cost Metrics" in bold
   - Row 12-13: "BEK Purchases", "Purchase Cost %"
   - Row 15: Section header "Labor" in bold
   - Row 16-17: "Estimated Labor Cost", "Labor Cost %"
   - Row 19: Section header "Volume" in bold
   - Row 20-21: "Total Orders", "Avg Ticket Size"
   - Column B header: "This Week" | Column C: "Prior Week" | Column D: "WoW Change" | Column E: "WoW %"
   - Formulas will be added in Plan 02
   - Format column B-C as currency (where applicable), column E as percentage

8. **Summary tab** — STRUCTURE ONLY in this task:
   - Row 1: "Weekly Report Summary" merged, bold, 18pt
   - Row 2: "Week Ending:" label, empty date cell
   - Row 3: blank separator
   - Row 4: Column headers: blank | MML | MMA | MM3 | TS1 | TS2
   - Brand color bands: columns B-D (Moto Medi) get light orange background (FDE9D9), columns E-F (Tikka Shack) get light teal background (D0E2F1)
   - Row 5: "Net Revenue" label
   - Row 6: "  WoW" (indented) label
   - Row 7: "Purchase Cost %" label
   - Row 8: "  WoW" label
   - Row 9: "Labor Cost %" label
   - Row 10: " WoW" label
   - Row 11: "Order Volume" label
   - Row 12: " WoW" label
   - Row 13: "Avg Ticket" label
   - Row 14: " WoW" label
   - Freeze row 4 and column A
   - Column A width: 20, columns B-F width: 18
   - Formulas added in Plan 02

General formatting across all tabs:
- Font: Calibri 11pt (default — readable, professional, not "AI slop" for a spreadsheet tool)
- Header rows: bold, dark background (4472C4 for import tabs, 2F5496 for calc/summary), white text
- Alternating row colors where data exists (F2F2F2 / FFFFFF)
- Print area set to used range on Summary tab

Run the script at the end: `python3 scripts/build_weekly_report.py` and verify it produces the .xlsx without errors.
  </action>
  <verify>
    <automated>cd /Users/josi/crash-course-intake && python3 scripts/build_weekly_report.py && python3 -c "
import openpyxl
wb = openpyxl.load_workbook('Rez-Weekly-Report.xlsx')
expected_tabs = ['Instructions', 'Square-Import', 'DoorDash-Import', 'UberEats-Import', 'Grubhub-Import', 'BEK-Import', 'Employee-Roster', 'Prior-Week', 'MML-Calc', 'MMA-Calc', 'MM3-Calc', 'TS1-Calc', 'TS2-Calc', 'Summary']
actual_tabs = wb.sheetnames
assert actual_tabs == expected_tabs, f'Tab mismatch: {actual_tabs}'
# Check import tabs have headers and validation row
for tab in ['Square-Import', 'DoorDash-Import', 'UberEats-Import', 'Grubhub-Import', 'BEK-Import']:
    ws = wb[tab]
    assert ws.cell(1, 1).value is not None, f'{tab} missing headers'
    assert ws.cell(2, 1).value is not None or ws.cell(2, 2).value is not None, f'{tab} missing validation row'
# Check Employee-Roster has data
assert wb['Employee-Roster'].cell(2, 1).value is not None, 'Roster empty'
# Check Prior-Week has headers
assert wb['Prior-Week'].cell(1, 1).value is not None, 'Prior-Week missing headers'
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <done>
    - Script runs without errors and produces Rez-Weekly-Report.xlsx
    - All 14 tabs exist in correct order with correct tab colors
    - Each import tab has placeholder headers, validation row formulas, and sample data
    - Instructions tab has step-by-step platform instructions
    - Employee-Roster has sample employees with Square Name column
    - Prior-Week has date-keyed structure with sample data
    - Location Calc tabs have metric label structure (no formulas yet)
    - Summary tab has layout skeleton with brand color bands (no formulas yet)
  </done>
</task>

</tasks>

<verification>
- `python3 scripts/build_weekly_report.py` runs cleanly
- Output .xlsx opens in Excel/Google Sheets with all 14 tabs visible
- Import tabs show placeholder headers with validation row
- Tab colors distinguish import (blue), calc (gray), summary (amber), reference (green)
</verification>

<success_criteria>
The foundation workbook structure is complete — all tabs exist with correct layouts, sample data is populated for testing, and the import tab pattern (headers + validation + paste zone) is established. Plan 02 can add formulas that reference these tabs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-weekly-reporting/01-01-SUMMARY.md`
</output>
